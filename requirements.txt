# Core. torch is intentionally unpinned to a build: on Colab/Kaggle/molab the
# preinstalled CUDA build already satisfies it, and pinning here would risk
# pip swapping in a CPU wheel.
torch>=2.2
numpy
pretty_midi
soundfile
pyyaml
tqdm
pytest>=8.0
